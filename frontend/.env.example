# ─── ERC-4337 V2 frontend (Vite) ───────────────────────────────────────
# Copy to frontend/.env and fill in. Every VITE_* var is shipped to the BROWSER, so NEVER put a
# private key here — the signing key is DERIVED in the browser from the answers.

# Sepolia RPC (or http://127.0.0.1:8545 for a local Anvil fork)
VITE_RPC_URL=

# Bundler base URL (JSON-RPC at /, deploy at /deploy)
VITE_BUNDLER_URL=http://localhost:3000

# EntryPoint v0.8 (do not change on Sepolia)
VITE_ENTRYPOINT_ADDRESS=0x4337084d9e255ff0702461cf8895ce9e3b5ff108

# Deployed V2 demo SecretQuestionAccount (signer = derive("rex","paris","inception"))
VITE_DEMO_ACCOUNT_ADDRESS=

# Paymaster + Counter from the V2 deployment
VITE_PAYMASTER_ADDRESS=
VITE_COUNTER_ADDRESS=

# Optional: override the derivation salt (defaults to the lib's public DEFAULT_SALT)
# VITE_SALT=

# Block explorer base (Sepolia)
VITE_EXPLORER_URL=https://sepolia.etherscan.io
